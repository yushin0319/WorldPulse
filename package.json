{
  "name": "worldpulse",
  "version": "1.0.0",
  "private": true,
  "description": "世界各地のニュースをリアルタイムに地図上で可視化するWebアプリケーション。",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,json,css}": [
      "npx @biomejs/biome check --write --no-errors-on-unmatched"
    ]
  }
}
